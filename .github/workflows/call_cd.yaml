name: cd

on:
  workflow_call:
    inputs:
      environment-runs-on:
        required: true
        type: string
jobs:
  cd-to-demo-dev:
    runs-on: ${{ inputs.environment-runs-on }}
    steps:
      - name: cd
        run: |
              echo "${{ inputs.environment-runs-on }}"
